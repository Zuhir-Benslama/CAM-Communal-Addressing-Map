"""Lookup table models — all moved to JSON-based loading.

Static lookup data (road types, zone types, etc.) is now loaded
from template_data/*.json via scripts.lookup_data at runtime.
This file remains as an empty placeholder to avoid breaking imports
that reference this module.
"""
