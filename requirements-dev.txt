# Dev / quality tooling — exact pins so CI matches the maintainer's
# environment. Bump deliberately and keep the pins in sync with whatever
# works locally.
-r requirements.txt

ruff==0.16.4
mypy==2.1.0
pytest==9.0.3
pytest-cov==7.1.0
pyqt6==6.11.0